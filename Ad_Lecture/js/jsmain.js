var sticker = []; // nhan dan luu tru mssv
let faceDetected = []; // luu cac 
let sendingData = false; // ngan chan nhieu lan khi ghi nhanj diem danh

function updateTable() {
    var selectedCourseID = document.getElementById('select-course').value
    var selectedUnitcode = document.getElementById('select-unit').value;
    var selectedVenue = document.getElementById('select-venue').value;

    var xhr = new XMLHttpRequest
}